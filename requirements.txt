# TP1 (G2) — RSNA 2020 Pulmonary Embolism Detection — baseline clássico
# Instalar com: pip install -r requirements.txt
# Versões fixadas por faixa compatível (não travadas em patch) para reprodutibilidade
# sem engessar demais o ambiente (enunciado §4.5).

numpy>=1.26,<3
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
scikit-image>=0.22
imbalanced-learn>=0.12
xgboost>=2.0
lightgbm>=4.3

pydicom>=2.4
SimpleITK>=2.3
pyradiomics==3.0.1     # 3.1.0 tem metadata quebrada no PyPI (sdist reporta versão errada)

matplotlib>=3.8
seaborn>=0.13

pyarrow>=15.0          # backend parquet para pandas
joblib>=1.3
tqdm>=4.66

jupyter>=1.0
notebook>=7.0

pytest>=8.0
